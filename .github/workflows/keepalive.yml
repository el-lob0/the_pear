
name: Keep Render Bot Awake
on:
  schedule:
    - cron: "*/5 * * * *"  # every 5 minutes
  workflow_dispatch:        # allow manual run

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bot
        run: curl -fsS https://your-app-name.onrender.com || echo "Failed to ping"
